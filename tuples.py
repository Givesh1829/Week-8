#Tuples: are fixed and can't be changed.

my_tuple = (1, 2, 3, 4, 5) #uses round brackets.

print(my_tuple)
print(my_tuple[0])
print(my_tuple[2])
print(my_tuple[-1]) #negative 1 will always index the last value of the list. even in (lists.py).

tuple1 = (1, 2, 3)
tuple2 = (4, 5, 6)

conc_tuple = tuple1 + tuple2 #this can be used in (lists.py) as well.
rep_tuple = tuple1 * 3 #if you want to repeat the value.

print(rep_tuple)

